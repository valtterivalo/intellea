# local development

## run

```bash
pnpm install
pnpm dev
```

## urls

- docs: `http://localhost:3000/docs`
- embed shell: `http://localhost:3000/embed/graph`

## optional

- set `NEXT_PUBLIC_DEBUG=true` to show the perf overlay
